---
# Keep node-only bootstrap settings scoped to the Kubernetes guests.
kubernetes_pod_network_cidr: 10.244.0.0/16
kubernetes_control_plane_advertise_address: 192.168.122.10

# Hold only the packages explicitly installed; dependencies (cri-tools, kubernetes-cni)
# are managed by apt and do not need to be held separately.
kubernetes_held_packages:
  - kubeadm
  - kubelet
  - kubectl
